# ============================================================
#  VPS Deploy — Configuration File
#  Copy this to .env and fill in your values before running: npm run setup
# ============================================================

# --- VPS Access ---
VPS_IP=your-vps-ip-here

# Authentication: Provide EITHER root password OR root SSH key (not both)
# Option 1: Use password authentication
ROOT_PASSWORD=your-root-password-here
# Option 2: Use SSH key authentication (recommended)
# SSH_KEY_PATH=$HOME/.ssh/your_root_key

# --- SSH Key ---
# Local path where the SSH key pair will be saved/used
SSH_KEY_PATH=your-ssh-key-path-here

# --- GitHub ---
GITHUB_REPO=https://github.com/your-username/your-repo
GITHUB_TOKEN=your-github-token-here

# --- App ---
# The folder name after git clone (usually the repo name)
APP_DIR_NAME=your-app-name
# The port your app exposes inside the container
APP_PORT=3000

# --- Firewall — Extra ports to allow (beyond 22/80/443) ---
# Comma-separated list of ports (or leave empty)
EXTRA_PORTS=

# --- Fail2Ban ---
FAIL2BAN_BANTIME=3600
FAIL2BAN_FINDTIME=600
FAIL2BAN_MAXRETRY=5

# --- SSL (Let's Encrypt) — used after deploy for nginx ---
# Email for Let's Encrypt (required for cert issuance)
LETSENCRYPT_EMAIL=your@email.com
# Primary cert: comma-separated domains (e.g. frontwizards.com,www.frontwizards.com)
# Cert will be at /etc/letsencrypt/live/<first-domain>/
PRIMARY_SSL_DOMAINS=frontwizards.com,www.frontwizards.com
# Optional second cert (e.g. hmellahi.me,frontendpractice.hmellahi.me)
ADDITIONAL_SSL_DOMAINS=hmellahi.me,frontendpractice.hmellahi.me
